# Release signing properties for local builds.
# Keep key.properties and the keystore outside version control.
storeFile=<KEYSTORE_PATH>
storePassword=<KEYSTORE_PASSWORD>
keyAlias=<KEY_ALIAS>
keyPassword=<KEY_PASSWORD>
storeType=pkcs12
